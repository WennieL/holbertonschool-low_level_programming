#include<stdio.h>

/**
 * main: use printf to print
 * with proper grammar, but the outcome is a piece of art,
 * followed by a new line
 *
 * Return: value 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
